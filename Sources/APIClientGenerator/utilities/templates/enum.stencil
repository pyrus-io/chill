//
//  🍦 Chill friend, this code is generated
//  🤔 Probably best not to touch it
//
import Foundation

public enum {{ enum.name }}: String, Codable, Equatable, Hashable, CaseIterable {
  {% for case in enum.cases %}
    case {{ case }}
  {% endfor %}
}
